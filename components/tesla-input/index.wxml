<view class="form-item">
  <view wx:if="{{label}}" class="label">{{label}}</view>
  <input value="{{value}}" bind:input="onInputChange" class="input {{isError ? 'error' :''}}" name="input" />
  <view wx:if="{{isError}}" class="error-message">请填写{{label || '必要字段'}}</view>
</view>